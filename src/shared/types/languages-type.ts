type Header = {
  about: string
  experiences: string
  projects: string
}

type Body = {
  about: string
  experiences: string
  projects: string
}

type Footer = {
  socialMedia: "Minha mídias sociais: "
}

export type LanguageType = {
  header: Header
  body: Body
  footer: Footer
}
